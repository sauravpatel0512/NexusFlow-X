FROM spark:python3

# Writable Ivy/pip cache when host injects a bad HOME
ENV HOME=/tmp
ENV JAVA_TOOL_OPTIONS=-Duser.home=/tmp

RUN pip install --no-cache-dir \
    kafka-python \
    pyyaml

WORKDIR /app
CMD ["sleep", "infinity"]
